# Phase 06 — Mobile compatibility

Update Expo client paths and copy to the canonical campus assistant contract, preserve auth/error behavior, and add regression assertions for JSON/SSE-compatible request shapes. Run mobile tests and typecheck. Real Android/iOS device and store-release testing remain explicitly deferred until after the web release.

Exit criterion: source/API compatibility passes and the deferred device gate is documented.
