# Phase 01 — Foundation and ownership

1. Fetch `origin` and freeze the actual base SHA. If it differs from the recorded expected base, update the ledger and rerun the consistency sweep.
2. Verify the recovery patch/checksums and keep the original dirty checkout and all existing worktrees untouched.
3. Freeze API/data contracts before parallel writers: canonical assistant paths, deprecated thesis aliases, citation domains, Supabase release manifest, active-release pointer, and secret-file names.
4. Create disjoint worktrees for FE, backend/RAG, platform and later mobile work. Workers do not merge, push, or spawn nested agents.

Exit criterion: ledger names active plan, base SHA, owners, forbidden paths, next step and exact test command; Kongming returns an independent architecture result.
