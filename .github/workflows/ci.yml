name: CI Build and Test

on:
  push:
    branches: [master, main]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [master, main]

permissions:
  contents: read
  pull-requests: read
  security-events: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  architecture-guard:
    name: architecture-guard
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: packages/platform-auth/package-lock.json

      - name: Install shared auth package dependencies
        working-directory: packages/platform-auth
        run: npm ci

      - name: Test shared auth package
        working-directory: packages/platform-auth
        run: npm test

      - name: Enforce architecture guard
        run: node scripts/check-architecture.mjs

      - name: Enforce documentation hygiene guard
        run: node scripts/check-doc-hygiene.mjs

  java-thesis-quality:
    name: java-thesis-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check thesis Java and frontend contract
        run: node scripts/check-thesis-contract.mjs

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          cache-dependency-path: |
            java-services/pom.xml
            java-services/thesis-service/pom.xml

      - name: Verify Spring Java services
        run: mvn -f java-services/pom.xml verify

  core-quality:
    name: core-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Generate Prisma Client
        working-directory: backend
        run: npm run prisma:generate

      - name: Lint
        working-directory: backend
        run: npm run lint

      - name: Check formatting
        working-directory: backend
        run: npm run lint:format

      - name: Typecheck
        working-directory: backend
        run: npm run typecheck

      - name: Build
        working-directory: backend
        run: npm run build

      - name: Unit tests
        working-directory: backend
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: npm run test:unit -- --runInBand

  core-integration:
    name: core-integration
    runs-on: ubuntu-latest
    needs: core-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 6379:6379
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Generate Prisma Client
        working-directory: backend
        run: npm run prisma:generate

      - name: Push Prisma schema
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npx prisma db push --skip-generate

      - name: Seed database
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npm run prisma:seed

      - name: Integration tests
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npm run test:integration -- --runInBand

      - name: Upload core integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: core-integration-logs
          path: |
            backend/coverage
            backend/test-results

  auth-quality:
    name: auth-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: auth-service/package-lock.json

      - name: Install dependencies
        working-directory: auth-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: auth-service
        run: npm run prisma:generate

      - name: Lint
        working-directory: auth-service
        run: npm run lint

      - name: Check formatting
        working-directory: auth-service
        run: npm run lint:format

      - name: Typecheck
        working-directory: auth-service
        run: npm run typecheck

      - name: Build
        working-directory: auth-service
        run: npm run build

      - name: Unit tests
        working-directory: auth-service
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: npm run test:unit -- --runInBand

  auth-integration:
    name: auth-integration
    runs-on: ubuntu-latest
    needs: auth-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 6379:6379
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            auth-service/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Install auth-service dependencies
        working-directory: auth-service
        run: npm ci

      - name: Generate backend Prisma Client
        working-directory: backend
        run: npm run prisma:generate

      - name: Generate auth-service Prisma Client
        working-directory: auth-service
        run: npm run prisma:generate

      - name: Push core public schema
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npx prisma db push --skip-generate

      - name: Seed core public schema
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npm run prisma:seed

      - name: Push auth schema
        working-directory: auth-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=auth
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npx prisma db push --skip-generate

      - name: Migrate legacy auth data into auth schema
        working-directory: auth-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=auth
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npm run data:migrate:legacy

      - name: Integration tests
        working-directory: auth-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=auth
          REDIS_URL: redis://127.0.0.1:6379
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          JWT_REFRESH_SECRET: ci-refresh-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npm run test:integration -- --runInBand

      - name: Upload auth integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: auth-integration-logs
          path: |
            auth-service/coverage
            auth-service/test-results

  notification-quality:
    name: notification-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: notification-service/package-lock.json

      - name: Install dependencies
        working-directory: notification-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: notification-service
        run: npm run prisma:generate

      - name: Lint
        working-directory: notification-service
        run: npm run lint

      - name: Check formatting
        working-directory: notification-service
        run: npm run lint:format

      - name: Typecheck
        working-directory: notification-service
        run: npm run typecheck

      - name: Build
        working-directory: notification-service
        run: npm run build

      - name: Unit tests
        working-directory: notification-service
        run: npm run test:unit -- --runInBand

  notification-integration:
    name: notification-integration
    runs-on: ubuntu-latest
    needs: notification-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: notification-service/package-lock.json

      - name: Install dependencies
        working-directory: notification-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: notification-service
        run: npm run prisma:generate

      - name: Push Prisma schema
        working-directory: notification-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=notifications
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npx prisma db push --skip-generate

      - name: Integration tests
        working-directory: notification-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=notifications
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npm run test:integration -- --runInBand

      - name: Upload notification integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: notification-integration-logs
          path: |
            notification-service/coverage
            notification-service/test-results

  finance-quality:
    name: finance-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: finance-service/package-lock.json

      - name: Install dependencies
        working-directory: finance-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: finance-service
        run: npm run prisma:generate

      - name: Lint
        working-directory: finance-service
        run: npm run lint

      - name: Check formatting
        working-directory: finance-service
        run: npm run lint:format

      - name: Typecheck
        working-directory: finance-service
        run: npm run typecheck

      - name: Build
        working-directory: finance-service
        run: npm run build

      - name: Unit tests
        working-directory: finance-service
        run: npm run test:unit -- --runInBand

  finance-integration:
    name: finance-integration
    runs-on: ubuntu-latest
    needs: finance-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: finance-service/package-lock.json

      - name: Install dependencies
        working-directory: finance-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: finance-service
        run: npm run prisma:generate

      - name: Push Prisma schema
        working-directory: finance-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=finance
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npx prisma db push --skip-generate

      - name: Integration tests
        working-directory: finance-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=finance
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npm run test:integration -- --runInBand

      - name: Upload finance integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: finance-integration-logs
          path: |
            finance-service/coverage
            finance-service/test-results

  academic-quality:
    name: academic-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: academic-service/package-lock.json

      - name: Install dependencies
        working-directory: academic-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: academic-service
        run: npm run prisma:generate

      - name: Lint
        working-directory: academic-service
        run: npm run lint

      - name: Check formatting
        working-directory: academic-service
        run: npm run lint:format

      - name: Typecheck
        working-directory: academic-service
        run: npm run typecheck

      - name: Build
        working-directory: academic-service
        run: npm run build

      - name: Unit tests
        working-directory: academic-service
        run: npm run test:unit -- --runInBand

  academic-integration:
    name: academic-integration
    runs-on: ubuntu-latest
    needs: academic-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: academic-service/package-lock.json

      - name: Install dependencies
        working-directory: academic-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: academic-service
        run: npm run prisma:generate

      - name: Push Prisma schema
        working-directory: academic-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=academic
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npx prisma db push --skip-generate

      - name: Integration tests
        working-directory: academic-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=academic
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npm run test:integration -- --runInBand

      - name: Upload academic integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: academic-integration-logs
          path: |
            academic-service/coverage
            academic-service/test-results

  engagement-quality:
    name: engagement-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: engagement-service/package-lock.json

      - name: Install dependencies
        working-directory: engagement-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: engagement-service
        run: npm run prisma:generate

      - name: Lint
        working-directory: engagement-service
        run: npm run lint

      - name: Check formatting
        working-directory: engagement-service
        run: npm run lint:format

      - name: Typecheck
        working-directory: engagement-service
        run: npm run typecheck

      - name: Build
        working-directory: engagement-service
        run: npm run build

      - name: Unit tests
        working-directory: engagement-service
        run: npm run test:unit -- --runInBand

  engagement-integration:
    name: engagement-integration
    runs-on: ubuntu-latest
    needs: engagement-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: engagement-service/package-lock.json

      - name: Install dependencies
        working-directory: engagement-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: engagement-service
        run: npm run prisma:generate

      - name: Push Prisma schema
        working-directory: engagement-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=engagement
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npx prisma db push --skip-generate

      - name: Integration tests
        working-directory: engagement-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=engagement
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npm run test:integration -- --runInBand

      - name: Upload engagement integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: engagement-integration-logs
          path: |
            engagement-service/coverage
            engagement-service/test-results

  people-quality:
    name: people-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: people-service/package-lock.json

      - name: Install dependencies
        working-directory: people-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: people-service
        run: npm run prisma:generate

      - name: Lint
        working-directory: people-service
        run: npm run lint

      - name: Check formatting
        working-directory: people-service
        run: npm run lint:format

      - name: Typecheck
        working-directory: people-service
        run: npm run typecheck

      - name: Build
        working-directory: people-service
        run: npm run build

      - name: Unit tests
        working-directory: people-service
        run: npm run test:unit -- --runInBand

  people-integration:
    name: people-integration
    runs-on: ubuntu-latest
    needs: people-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: people-service/package-lock.json

      - name: Install dependencies
        working-directory: people-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: people-service
        run: npm run prisma:generate

      - name: Push Prisma schema
        working-directory: people-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=people
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npx prisma db push --skip-generate

      - name: Integration tests
        working-directory: people-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=people
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
          INTERNAL_SERVICE_TOKEN: ci-internal-service-token
        run: npm run test:integration -- --runInBand

      - name: Upload people integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: people-integration-logs
          path: |
            people-service/coverage
            people-service/test-results

  analytics-quality:
    name: analytics-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: analytics-service/package-lock.json

      - name: Install dependencies
        working-directory: analytics-service
        run: npm ci

      - name: Generate Prisma Client
        working-directory: analytics-service
        run: npm run prisma:generate

      - name: Lint
        working-directory: analytics-service
        run: npm run lint

      - name: Check formatting
        working-directory: analytics-service
        run: npm run lint:format

      - name: Typecheck
        working-directory: analytics-service
        run: npm run typecheck

      - name: Build
        working-directory: analytics-service
        run: npm run build

      - name: Unit tests
        working-directory: analytics-service
        run: npm run test:unit -- --runInBand

  analytics-integration:
    name: analytics-integration
    runs-on: ubuntu-latest
    needs: analytics-quality

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: campuscore
          POSTGRES_USER: campuscore
          POSTGRES_PASSWORD: ci-postgres-password
        options: >-
          --health-cmd "pg_isready -U campuscore -d campuscore"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
        ports:
          - 5432:5432
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: campuscore
          RABBITMQ_DEFAULT_PASS: ci-rabbitmq-password
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 30
        ports:
          - 5672:5672

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            analytics-service/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Install analytics service dependencies
        working-directory: analytics-service
        run: npm ci

      - name: Generate backend Prisma Client
        working-directory: backend
        run: npm run prisma:generate

      - name: Generate analytics Prisma Client
        working-directory: analytics-service
        run: npm run prisma:generate

      - name: Push public schema
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npx prisma db push --skip-generate

      - name: Seed database
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
        run: npm run prisma:seed

      - name: Integration tests
        working-directory: analytics-service
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://campuscore:ci-postgres-password@127.0.0.1:5432/campuscore?schema=public
          RABBITMQ_URL: amqp://campuscore:ci-rabbitmq-password@127.0.0.1:5672
          FRONTEND_URL: http://127.0.0.1
          JWT_SECRET: ci-jwt-secret
          HEALTH_READINESS_KEY: ci-health-readiness-key
          COOKIE_SECURE: 'false'
        run: npm run test:integration -- --runInBand

      - name: Upload analytics integration logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: analytics-integration-logs
          path: |
            analytics-service/coverage
            analytics-service/test-results

  frontend-quality:
    name: frontend-quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Lint
        working-directory: frontend
        run: npm run lint

      - name: Typecheck
        working-directory: frontend
        run: npm run typecheck

      - name: Test
        working-directory: frontend
        run: npm test

      - name: Build
        working-directory: frontend
        run: npm run build

  frontend-fast-e2e:
    name: frontend-fast-e2e
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs:
      [
        core-quality,
        auth-quality,
        notification-quality,
        finance-quality,
        academic-quality,
        engagement-quality,
        people-quality,
        analytics-quality,
        frontend-quality,
      ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            auth-service/package-lock.json
            notification-service/package-lock.json
            finance-service/package-lock.json
            academic-service/package-lock.json
            engagement-service/package-lock.json
            people-service/package-lock.json
            analytics-service/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Install auth-service dependencies
        working-directory: auth-service
        run: npm ci

      - name: Install notification service dependencies
        working-directory: notification-service
        run: npm ci

      - name: Install finance service dependencies
        working-directory: finance-service
        run: npm ci

      - name: Install academic service dependencies
        working-directory: academic-service
        run: npm ci

      - name: Install engagement service dependencies
        working-directory: engagement-service
        run: npm ci

      - name: Install people service dependencies
        working-directory: people-service
        run: npm ci

      - name: Install analytics service dependencies
        working-directory: analytics-service
        run: npm ci

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Install Playwright browsers
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Run fast E2E
        working-directory: frontend
        run: npm run test:e2e

      - name: Upload fast E2E artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fast-e2e-artifacts
          path: |
            frontend/playwright-report
            frontend/test-results

  compose-contract:
    name: compose-contract
    runs-on: ubuntu-latest
    env:
      POSTGRES_PASSWORD: ci-postgres-password
      JWT_SECRET: ci-jwt-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
      HEALTH_READINESS_KEY: ci-health-readiness-key
      INTERNAL_SERVICE_TOKEN: ci-internal-service-token
      RABBITMQ_USER: campuscore
      RABBITMQ_PASSWORD: ci-rabbitmq-password
      MINIO_USER: campuscore
      MINIO_PASSWORD: ci-minio-password
      DOCKERHUB_NAMESPACE: ci-namespace
      IMAGE_TAG: v9.9.9
      FRONTEND_URL: http://localhost

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate docker-compose.yml
        run: docker compose -f docker-compose.yml config > /dev/null

      - name: Validate docker-compose.production.yml with preflight
        run: node scripts/check-production-compose-config.mjs

      - name: Validate docker-compose.e2e.yml
        run: docker compose -f docker-compose.e2e.yml config > /dev/null

      - name: Check diff whitespace
        run: git diff --check

  k8s-contract:
    name: k8s-contract
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.31.0

      - name: Render runtime Kustomize base
        run: kubectl kustomize k8s/base > /dev/null

      - name: Render bootstrap Kustomize base
        run: kubectl kustomize k8s/bootstrap > /dev/null

      - name: Render Docker Desktop Kubernetes overlay
        run: kubectl kustomize k8s/overlays/docker-desktop > /dev/null

      - name: Render isolated Java thesis pilot overlay
        run: kubectl kustomize k8s/overlays/thesis-pilot > /dev/null

      - name: Render staging generic Kubernetes overlay
        run: kubectl kustomize k8s/overlays/staging-generic > /dev/null

      - name: Render production generic Kubernetes overlay
        run: kubectl kustomize k8s/overlays/prod-generic > /dev/null

      - name: Render staging operator Kubernetes overlay
        run: kubectl kustomize k8s/overlays/staging-operator > /dev/null

      - name: Render production operator Kubernetes overlay
        run: kubectl kustomize k8s/overlays/prod-operator > /dev/null

      - name: Run Kubernetes preflight guard
        run: node scripts/run-k8s-preflight.mjs

  image-smoke:
    name: image-smoke
    runs-on: ubuntu-latest
    needs:
      [
        core-quality,
        core-integration,
        auth-quality,
        auth-integration,
        notification-quality,
        notification-integration,
        finance-quality,
        finance-integration,
        academic-quality,
        academic-integration,
        engagement-quality,
        engagement-integration,
        people-quality,
        people-integration,
        analytics-quality,
        analytics-integration,
        frontend-quality,
        frontend-fast-e2e,
        compose-contract,
        k8s-contract,
      ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run image smoke
        run: node scripts/run-image-smoke.mjs

      - name: Upload image smoke logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: image-smoke-logs
          path: frontend/test-results/image-smoke-stack

  edge-e2e:
    name: edge-e2e
    runs-on: ubuntu-latest
    needs:
      [
        core-quality,
        core-integration,
        auth-quality,
        auth-integration,
        notification-quality,
        notification-integration,
        finance-quality,
        finance-integration,
        academic-quality,
        academic-integration,
        engagement-quality,
        engagement-integration,
        people-quality,
        people-integration,
        analytics-quality,
        analytics-integration,
        frontend-quality,
        frontend-fast-e2e,
        compose-contract,
        k8s-contract,
      ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Install shared auth package dependencies
        working-directory: packages/platform-auth
        run: npm ci

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Install Playwright browsers
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Run edge E2E
        working-directory: frontend
        run: npm run test:e2e:edge

      - name: Upload edge stack logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: edge-e2e-stack-logs
          path: frontend/test-results/edge-e2e-stack

      - name: Upload edge E2E artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: edge-e2e-artifacts
          path: |
            frontend/playwright-report
            frontend/test-results

  security-scan:
    name: security-scan
    runs-on: ubuntu-latest
    needs:
      [
        core-quality,
        core-integration,
        auth-quality,
        auth-integration,
        notification-quality,
        notification-integration,
        finance-quality,
        finance-integration,
        academic-quality,
        academic-integration,
        engagement-quality,
        engagement-integration,
        people-quality,
        people-integration,
        analytics-quality,
        analytics-integration,
        frontend-quality,
        frontend-fast-e2e,
        compose-contract,
        k8s-contract,
      ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            packages/platform-auth/package-lock.json
            backend/package-lock.json
            auth-service/package-lock.json
            notification-service/package-lock.json
            finance-service/package-lock.json
            academic-service/package-lock.json
            engagement-service/package-lock.json
            people-service/package-lock.json
            analytics-service/package-lock.json
            frontend/package-lock.json

      - name: Prepare scan directory
        run: mkdir -p security-results

      - name: Gitleaks repo scan
        id: gitleaks
        continue-on-error: true
        uses: gacts/gitleaks@v1
        with:
          path: .

      - name: Collect gitleaks SARIF
        if: always() && steps.gitleaks.outputs.sarif != ''
        run: cp "${{ steps.gitleaks.outputs.sarif }}" security-results/gitleaks.sarif

      - name: Install shared auth package dependencies
        working-directory: packages/platform-auth
        run: npm ci

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Install auth-service dependencies
        working-directory: auth-service
        run: npm ci

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Install notification service dependencies
        working-directory: notification-service
        run: npm ci

      - name: Install finance service dependencies
        working-directory: finance-service
        run: npm ci

      - name: Install academic service dependencies
        working-directory: academic-service
        run: npm ci

      - name: Install engagement service dependencies
        working-directory: engagement-service
        run: npm ci

      - name: Install people service dependencies
        working-directory: people-service
        run: npm ci

      - name: Install analytics service dependencies
        working-directory: analytics-service
        run: npm ci

      - name: Platform auth npm audit
        id: platform-auth-npm-audit
        continue-on-error: true
        working-directory: packages/platform-auth
        run: npm audit --audit-level=high --json > ../../security-results/platform-auth-npm-audit.json 2> ../../security-results/platform-auth-npm-audit.stderr.log

      - name: Backend npm audit
        id: backend-npm-audit
        continue-on-error: true
        working-directory: backend
        run: npm audit --audit-level=high --json > ../security-results/backend-npm-audit.json 2> ../security-results/backend-npm-audit.stderr.log

      - name: Frontend npm audit
        id: frontend-npm-audit
        continue-on-error: true
        working-directory: frontend
        run: npm audit --audit-level=high --json > ../security-results/frontend-npm-audit.json 2> ../security-results/frontend-npm-audit.stderr.log

      - name: Auth service npm audit
        id: auth-npm-audit
        continue-on-error: true
        working-directory: auth-service
        run: npm audit --audit-level=high --json > ../security-results/auth-npm-audit.json 2> ../security-results/auth-npm-audit.stderr.log

      - name: Notification service npm audit
        id: notification-npm-audit
        continue-on-error: true
        working-directory: notification-service
        run: npm audit --audit-level=high --json > ../security-results/notification-npm-audit.json 2> ../security-results/notification-npm-audit.stderr.log

      - name: Finance service npm audit
        id: finance-npm-audit
        continue-on-error: true
        working-directory: finance-service
        run: npm audit --audit-level=high --json > ../security-results/finance-npm-audit.json 2> ../security-results/finance-npm-audit.stderr.log

      - name: Academic service npm audit
        id: academic-npm-audit
        continue-on-error: true
        working-directory: academic-service
        run: npm audit --audit-level=high --json > ../security-results/academic-npm-audit.json 2> ../security-results/academic-npm-audit.stderr.log

      - name: Engagement service npm audit
        id: engagement-npm-audit
        continue-on-error: true
        working-directory: engagement-service
        run: npm audit --audit-level=high --json > ../security-results/engagement-npm-audit.json 2> ../security-results/engagement-npm-audit.stderr.log

      - name: People service npm audit
        id: people-npm-audit
        continue-on-error: true
        working-directory: people-service
        run: npm audit --audit-level=high --json > ../security-results/people-npm-audit.json 2> ../security-results/people-npm-audit.stderr.log

      - name: Analytics service npm audit
        id: analytics-npm-audit
        continue-on-error: true
        working-directory: analytics-service
        run: npm audit --audit-level=high --json > ../security-results/analytics-npm-audit.json 2> ../security-results/analytics-npm-audit.stderr.log

      - name: Platform auth filesystem scan
        id: trivy-platform-auth-fs
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: packages/platform-auth
          format: sarif
          output: security-results/trivy-platform-auth-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Filesystem scan
        id: trivy-fs
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: .
          format: sarif
          output: security-results/trivy-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Auth service filesystem scan
        id: trivy-auth-fs
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: auth-service
          format: sarif
          output: security-results/trivy-auth-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Academic service filesystem scan
        id: trivy-academic-fs
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: academic-service
          format: sarif
          output: security-results/trivy-academic-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Engagement service filesystem scan
        id: trivy-engagement-fs
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: engagement-service
          format: sarif
          output: security-results/trivy-engagement-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: People service filesystem scan
        id: trivy-people-fs
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: people-service
          format: sarif
          output: security-results/trivy-people-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Analytics service filesystem scan
        id: trivy-analytics-fs
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: fs
          scan-ref: analytics-service
          format: sarif
          output: security-results/trivy-analytics-fs.sarif
          severity: HIGH,CRITICAL
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Build backend image for scan
        run: docker build -f backend/Dockerfile -t campuscore-backend:security .

      - name: Build auth service image for scan
        run: docker build -f auth-service/Dockerfile -t campuscore-auth-service:security .

      - name: Build frontend image for scan
        run: docker build -f frontend/Dockerfile -t campuscore-frontend:security .

      - name: Build notification service image for scan
        run: docker build -f notification-service/Dockerfile -t campuscore-notification-service:security .

      - name: Build finance service image for scan
        run: docker build -f finance-service/Dockerfile -t campuscore-finance-service:security .

      - name: Build academic service image for scan
        run: docker build -f academic-service/Dockerfile -t campuscore-academic-service:security .

      - name: Build engagement service image for scan
        run: docker build -f engagement-service/Dockerfile -t campuscore-engagement-service:security .

      - name: Build people service image for scan
        run: docker build -f people-service/Dockerfile -t campuscore-people-service:security .

      - name: Build analytics service image for scan
        run: docker build -f analytics-service/Dockerfile -t campuscore-analytics-service:security .

      - name: Backend image scan
        id: trivy-backend-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-backend:security
          format: sarif
          output: security-results/trivy-backend-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Auth service image scan
        id: trivy-auth-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-auth-service:security
          format: sarif
          output: security-results/trivy-auth-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Frontend image scan
        id: trivy-frontend-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-frontend:security
          format: sarif
          output: security-results/trivy-frontend-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Notification service image scan
        id: trivy-notification-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-notification-service:security
          format: sarif
          output: security-results/trivy-notification-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Finance service image scan
        id: trivy-finance-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-finance-service:security
          format: sarif
          output: security-results/trivy-finance-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Academic service image scan
        id: trivy-academic-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-academic-service:security
          format: sarif
          output: security-results/trivy-academic-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Engagement service image scan
        id: trivy-engagement-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-engagement-service:security
          format: sarif
          output: security-results/trivy-engagement-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: People service image scan
        id: trivy-people-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-people-service:security
          format: sarif
          output: security-results/trivy-people-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Analytics service image scan
        id: trivy-analytics-image
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: image
          image-ref: campuscore-analytics-service:security
          format: sarif
          output: security-results/trivy-analytics-image.sarif
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          limit-severities-for-sarif: true

      - name: Upload gitleaks SARIF
        if: always() && hashFiles('security-results/gitleaks.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/gitleaks.sarif
          category: gitleaks

      - name: Upload platform auth filesystem SARIF
        if: always() && hashFiles('security-results/trivy-platform-auth-fs.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-platform-auth-fs.sarif
          category: trivy-platform-auth-fs

      - name: Upload filesystem SARIF
        if: always() && hashFiles('security-results/trivy-fs.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-fs.sarif
          category: trivy-fs

      - name: Upload backend image SARIF
        if: always() && hashFiles('security-results/trivy-backend-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-backend-image.sarif
          category: trivy-backend-image

      - name: Upload auth service filesystem SARIF
        if: always() && hashFiles('security-results/trivy-auth-fs.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-auth-fs.sarif
          category: trivy-auth-fs

      - name: Upload auth service image SARIF
        if: always() && hashFiles('security-results/trivy-auth-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-auth-image.sarif
          category: trivy-auth-image

      - name: Upload frontend image SARIF
        if: always() && hashFiles('security-results/trivy-frontend-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-frontend-image.sarif
          category: trivy-frontend-image

      - name: Upload notification image SARIF
        if: always() && hashFiles('security-results/trivy-notification-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-notification-image.sarif
          category: trivy-notification-image

      - name: Upload finance image SARIF
        if: always() && hashFiles('security-results/trivy-finance-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-finance-image.sarif
          category: trivy-finance-image

      - name: Upload academic filesystem SARIF
        if: always() && hashFiles('security-results/trivy-academic-fs.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-academic-fs.sarif
          category: trivy-academic-fs

      - name: Upload academic image SARIF
        if: always() && hashFiles('security-results/trivy-academic-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-academic-image.sarif
          category: trivy-academic-image

      - name: Upload engagement filesystem SARIF
        if: always() && hashFiles('security-results/trivy-engagement-fs.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-engagement-fs.sarif
          category: trivy-engagement-fs

      - name: Upload engagement image SARIF
        if: always() && hashFiles('security-results/trivy-engagement-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-engagement-image.sarif
          category: trivy-engagement-image

      - name: Upload people filesystem SARIF
        if: always() && hashFiles('security-results/trivy-people-fs.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-people-fs.sarif
          category: trivy-people-fs

      - name: Upload people image SARIF
        if: always() && hashFiles('security-results/trivy-people-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-people-image.sarif
          category: trivy-people-image

      - name: Upload analytics filesystem SARIF
        if: always() && hashFiles('security-results/trivy-analytics-fs.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-analytics-fs.sarif
          category: trivy-analytics-fs

      - name: Upload analytics image SARIF
        if: always() && hashFiles('security-results/trivy-analytics-image.sarif') != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-results/trivy-analytics-image.sarif
          category: trivy-analytics-image

      - name: Upload scan reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-scan-reports
          path: security-results

      - name: Enforce scan gate
        if: always()
        run: |
          if [ "${{ steps.gitleaks.outcome }}" != "success" ] || \
             [ "${{ steps.platform-auth-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.backend-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.auth-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.frontend-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.notification-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.finance-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.academic-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.engagement-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.people-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.analytics-npm-audit.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-platform-auth-fs.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-fs.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-auth-fs.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-academic-fs.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-engagement-fs.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-people-fs.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-analytics-fs.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-backend-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-auth-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-frontend-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-notification-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-finance-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-academic-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-engagement-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-people-image.outcome }}" != "success" ] || \
             [ "${{ steps.trivy-analytics-image.outcome }}" != "success" ]; then
            echo "Security scan failed. Review npm audit, gitleaks, and Trivy reports."
            exit 1
          fi

  dependency-review:
    name: dependency-review
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v4

      - name: Review dependency changes
        if: github.event_name == 'pull_request'
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high

      - name: Document push coverage
        if: github.event_name != 'pull_request'
        run: |
          {
            echo "## Dependency review"
            echo ""
            echo "Dependency Review compares dependency changes on pull requests only."
            echo "For push and semver tag runs, npm audit and Trivy coverage are enforced by the security-scan lane."
          } >> "$GITHUB_STEP_SUMMARY"

  quality-gate:
    name: quality-gate
    runs-on: ubuntu-latest
    needs:
      - architecture-guard
      - core-quality
      - core-integration
      - auth-quality
      - auth-integration
      - notification-quality
      - notification-integration
      - finance-quality
      - finance-integration
      - academic-quality
      - academic-integration
      - engagement-quality
      - engagement-integration
      - people-quality
      - people-integration
      - analytics-quality
      - analytics-integration
      - frontend-quality
      - frontend-fast-e2e
      - compose-contract
      - k8s-contract
      - image-smoke
      - edge-e2e
      - security-scan
      - dependency-review
    if: always()

    steps:
      - name: Assert all quality lanes passed
        run: |
          failures=0

          check_required() {
            local lane_name="$1"
            local lane_result="$2"

            if [ "$lane_result" != "success" ]; then
              echo "::error title=${lane_name} failed::${lane_name} result was ${lane_result}"
              failures=1
            fi
          }

          check_required architecture-guard "${{ needs.architecture-guard.result }}"
          check_required core-quality "${{ needs.core-quality.result }}"
          check_required core-integration "${{ needs.core-integration.result }}"
          check_required auth-quality "${{ needs.auth-quality.result }}"
          check_required auth-integration "${{ needs.auth-integration.result }}"
          check_required notification-quality "${{ needs.notification-quality.result }}"
          check_required notification-integration "${{ needs.notification-integration.result }}"
          check_required finance-quality "${{ needs.finance-quality.result }}"
          check_required finance-integration "${{ needs.finance-integration.result }}"
          check_required academic-quality "${{ needs.academic-quality.result }}"
          check_required academic-integration "${{ needs.academic-integration.result }}"
          check_required engagement-quality "${{ needs.engagement-quality.result }}"
          check_required engagement-integration "${{ needs.engagement-integration.result }}"
          check_required people-quality "${{ needs.people-quality.result }}"
          check_required people-integration "${{ needs.people-integration.result }}"
          check_required analytics-quality "${{ needs.analytics-quality.result }}"
          check_required analytics-integration "${{ needs.analytics-integration.result }}"
          check_required frontend-quality "${{ needs.frontend-quality.result }}"
          check_required frontend-fast-e2e "${{ needs.frontend-fast-e2e.result }}"
          check_required compose-contract "${{ needs.compose-contract.result }}"
          check_required k8s-contract "${{ needs.k8s-contract.result }}"
          check_required image-smoke "${{ needs.image-smoke.result }}"
          check_required edge-e2e "${{ needs.edge-e2e.result }}"
          check_required security-scan "${{ needs.security-scan.result }}"
          check_required dependency-review "${{ needs.dependency-review.result }}"

          if [ "$failures" -ne 0 ]; then
            exit 1
          fi

      - name: Summary
        run: |
          {
            echo "## Quality gate"
            echo ""
            echo "- architecture-guard: ${{ needs.architecture-guard.result }}"
            echo "- core-quality: ${{ needs.core-quality.result }}"
            echo "- core-integration: ${{ needs.core-integration.result }}"
            echo "- auth-quality: ${{ needs.auth-quality.result }}"
            echo "- auth-integration: ${{ needs.auth-integration.result }}"
            echo "- notification-quality: ${{ needs.notification-quality.result }}"
            echo "- notification-integration: ${{ needs.notification-integration.result }}"
            echo "- finance-quality: ${{ needs.finance-quality.result }}"
            echo "- finance-integration: ${{ needs.finance-integration.result }}"
            echo "- academic-quality: ${{ needs.academic-quality.result }}"
            echo "- academic-integration: ${{ needs.academic-integration.result }}"
            echo "- engagement-quality: ${{ needs.engagement-quality.result }}"
            echo "- engagement-integration: ${{ needs.engagement-integration.result }}"
            echo "- people-quality: ${{ needs.people-quality.result }}"
            echo "- people-integration: ${{ needs.people-integration.result }}"
            echo "- analytics-quality: ${{ needs.analytics-quality.result }}"
            echo "- analytics-integration: ${{ needs.analytics-integration.result }}"
            echo "- frontend-quality: ${{ needs.frontend-quality.result }}"
            echo "- frontend-fast-e2e: ${{ needs.frontend-fast-e2e.result }}"
            echo "- compose-contract: ${{ needs.compose-contract.result }}"
            echo "- k8s-contract: ${{ needs.k8s-contract.result }}"
            echo "- image-smoke: ${{ needs.image-smoke.result }}"
            echo "- edge-e2e: ${{ needs.edge-e2e.result }}"
            echo "- security-scan: ${{ needs.security-scan.result }}"
            echo "- dependency-review: ${{ needs.dependency-review.result }}"
          } >> "$GITHUB_STEP_SUMMARY"
