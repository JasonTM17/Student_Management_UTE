# MCP Management Scripts Environment Variables

# Path to MCP configuration file (optional, defaults to the active project adapter; Codex: .codex/.mcp.json)
MCP_CONFIG_PATH=.codex/.mcp.json

# Logging level (optional, defaults to info)
LOG_LEVEL=info

# Enable debug mode (optional, defaults to false)
DEBUG=false
